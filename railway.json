{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd backend && python3 -m pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && python3 -m gunicorn api:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
